{% set server = salt['grains.filter_by']({
    'Debian': {
        'pkgs': ['mongodb-org'],
        'service': 'mongod',
        'lock_dir': "/var/lock/mongod",
        'logging': {},
    }
}, merge=salt['pillar.get']('mongodb:server')) %}
